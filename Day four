/* let name="Saurabh\'";
console.log(name.length); //length is a property
                          // this consider \' as one letter output length is 8'

console.log(name.toUpperCase()); //toUpperCase function use
console.log(name.toLowerCase()); //toLowerCase function
console.log(name.slice(2,4)); //slice function with two parameters
console.log(name.slice(3)); //slice with one parameter
console.log(name.replace("abh","rya"));// replace function
let name1="Omkar"
console.log(name.concat("is a friend of ", name1)); //concat fun
let name2="      Saurabh    Dahibhate       "
console.log(name2.trim()); //trim fun */

//Practice set on String

//What will the following print in JavaScript? Console.log("har \"".length) 
console.log("har\"".length);
output will be 4 length of string


// Explore the includes, starts With & ends with functions of a string 
include() startwith() endwith() function
let text = "Hello world, welcome to the universe.";  

 let word= "Hello";
let result = text.includes(word);//include return true and false value
console.log( `${word}` + (result=true ? " is there " : "is not") + "in the sentence"); */


//Write a program to convert a given string to lowercase
let result=text.toLowerCase()
console.log(result); 

// Extract the amount out of this string
" Please give Rs 1000"
let text="Please give Rs 1000"
let result=text.slice(15);
console.log(result); */

/* Try to change 4th character of a given string. Were you alble to do it? No we can't able to do that because of strings are immutables */
